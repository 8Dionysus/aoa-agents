# Checkpoint Growth Part

This part routes growth status that must pass through reviewed checkpoint
posture inside `mechanics/rpg/`.

Current payload anchors remain in root districts and are listed in
`../../PARTS.md`.

Use parent validation in `../../README.md`. No payload is package-local here
yet.
