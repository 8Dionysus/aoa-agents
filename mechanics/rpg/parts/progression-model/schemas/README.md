# Progression Model Schemas

This directory owns machine-readable contracts for the `progression-model`
part.

| Schema | Role |
| --- | --- |
| [agent-progression.schema.json](agent-progression.schema.json) | evidence-backed progression overlay keyed by agent ID |

Stable schema `$id` values may preserve historical public identifiers. Active
repo lookup uses this part-local route.
