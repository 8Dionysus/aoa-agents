# Refresh Law Examples

These examples describe the bounded refresh-law route for generated Codex
subagent projection surfaces. They are route examples, not generated output
and not installed workspace state.

| Example | Purpose |
| --- | --- |
| [subagent-refresh-law.example.json](subagent-refresh-law.example.json) | Codex subagent projection refresh route |

Validate with:

```bash
python mechanics/codex-projection/parts/refresh-law/scripts/validate_codex_refresh_law_contracts.py
```
