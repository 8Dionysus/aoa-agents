# Eligibility Records

This directory holds source eligibility records for role-local specializations
that may later be reviewed for Codex custom-agent projection.

Each `*.eligibility.json` file is a candidate packet, not an install request.
Records here may be summarized into the generated readiness reader, but actual
projection still routes through `../subagent-projection/` in a later reviewed
builder change.

