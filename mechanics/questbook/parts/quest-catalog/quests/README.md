# Quest Catalog Records

This directory owns active YAML quest records for the `questbook` mechanic.

`scripts/validate_agents.py` reads these records, validates them against the
external quest schema when available, and checks the generated quest catalog
and dispatch readers.

Old root path lookup routes through
[../../../PROVENANCE.md](../../../PROVENANCE.md).
