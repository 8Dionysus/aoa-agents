interface:
  display_name: "AoA Agents"
  short_description: "Grow independent responsibility-bearing actors"
  default_prompt: "Use $aoa-agents-skills to detect an independent obligation and route only the actor-lifecycle stages it genuinely requires."

policy:
  allow_implicit_invocation: true

