interface:
  display_name: "AoA Summon"
  short_description: "Execute and return one bounded actor route"
  default_prompt: "Use $aoa-summon to decide or execute this complete external actor request through its inspected runtime binding and return the exact result; ordinary native Codex helpers do not enter this leaf."

policy:
  allow_implicit_invocation: false
