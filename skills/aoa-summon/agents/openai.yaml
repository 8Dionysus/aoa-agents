interface:
  display_name: "AoA Summon"
  short_description: "Execute and return one bounded actor route"
  default_prompt: "Use $aoa-summon only for a complete explicit delegation or an aoa-agents-skills external execution leaf, and preserve its exact runtime and responsibility return."

policy:
  allow_implicit_invocation: true
