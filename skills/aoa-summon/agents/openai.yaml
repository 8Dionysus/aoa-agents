interface:
  display_name: "AoA Summon"
  short_description: "Execute and return one bounded actor route"
  default_prompt: "Use $aoa-summon only after $aoa-agents-skills supplies a complete execution leaf, or for an explicitly requested disposable Codex-local child with a complete anchored return contract."

policy:
  allow_implicit_invocation: true
