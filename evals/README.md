# Local Eval Port

This skeleton `evals/` port will preserve agent-layer eval pressure around role
contracts, handoff posture, memory posture, checkpoint posture, and projection
boundaries.

`aoa-evals` owns central verdict, scoring, regression, and proof doctrine
authority. Agent profiles may be evaluated by proof surfaces, but local files
here do not become proof verdicts.
